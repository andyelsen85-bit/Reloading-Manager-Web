kind = "web"
previewPath = "/"
title = "Reloading Manager"
version = "1.0.0"
id = "artifacts/reloading-manager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22212

[services.development]
run = "pnpm --filter @workspace/reloading-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/reloading-manager", "run", "build" ]
publicDir = "artifacts/reloading-manager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22212"
BASE_PATH = "/"
